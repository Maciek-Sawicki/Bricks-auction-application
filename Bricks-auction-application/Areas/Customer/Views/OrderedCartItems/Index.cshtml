@model IEnumerable<Bricks_auction_application.Models.Users.OrderedCartItem>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OrderedCart)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Offer)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OrderedCart.OrderedCartId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Offer.OfferId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OrderedCartItemId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OrderedCartItemId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OrderedCartItemId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
