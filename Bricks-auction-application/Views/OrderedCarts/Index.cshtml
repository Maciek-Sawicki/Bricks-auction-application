@model IEnumerable<Bricks_auction_application.Models.Users.OrderedCart>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OrdersHistory)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OrdersHistory.OrderHistoryId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrderDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OrderedCartId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OrderedCartId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OrderedCartId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
